ROBOT MOVEMENT

const int LEFT_FORWARD = 6;
const int RIGHT_FORWARD = 5;
const int LEFT_BACKWARD = 11;
const int RIGHT_BACKWARD = 10;
const int ROTATOR_LEFT = 2;
const int ROTATOR_RIGHT = 3;

LINE SENSORS

GRAB ROBOT WITH YOUR RIGHT HAND AND FLIP IT. SENSORS ARE NUMBERED  FROM LEFT TO RIGHT FROM 1 TO 8.

  int SENSOR_1 = analogRead(A0);
  int SENSOR_2 = analogRead(A1);
  int SENSOR_3 = analogRead(A2);
  int SENSOR_4 = analogRead(A4);
  int SENSOR_5 = analogRead(A3);
  int SENSOR_6 = analogRead(A5);
  int SENSOR_7 = analogRead(A6);
  int SENSOR_8 = analogRead(A7);
